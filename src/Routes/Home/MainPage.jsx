import { Letter } from "../../Components";

const MainPage = () => {
  return (
    <div className="w-full h-screen flex items-center justify-center bg-gradient-to-r from-black to-gray-800">
      <Letter />
    </div>
  );
};

export default MainPage;
